//#include <stdio.h>
//#include <stdlib.h>
//
////树(tree)是n(n>=0)个结点的有限集。当n=0时成为空树，在任意一棵非空树中：
////有且仅有一个特定的称为根(root)的节点；
////当n>1时，其余节点可分为m(m>0)个互不相交的有限集T1、T2......Tm，其中
////每一个集合本身又是一棵树，并且称为根的子树(subtree)
////
////n>0时，根结点是唯一的，坚决不可能存在多个根结点。
////m>0 时，子树的个数是没有限制的，但他们互相是一定不会相交的
//
////一个存储结构设计得是否合理，取决于基于该存储结构的运算是否合适、
////是否方便、时间复杂度好不好等等。
//
//int main()
//{
//
//
//	system("pause");
//	return 0;
//}